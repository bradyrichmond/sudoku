// Module imports
import React, { Component } from 'react'

// App imports

const Square = ({value, classes}) => {
    return (
        <div className='square'>
            {value}
        </div>
    )
}

export default Square